services:
  closedbrowser:
    container_name: closedbrowser
    image: arranhs/closedbrowser:latest
    restart: unless-stopped
    shm_size: 1g # required to prevent crashes
    ports:
      - 9999:9999
    volumes:
      - ./data:/data
    environment:
      - TZ=Europe/London
