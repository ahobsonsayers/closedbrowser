diff --git a/blitzbrowser/src/components/browser-instance.component.ts b/blitzbrowser/src/components/browser-instance.component.ts
index 7f035f9..8410f56 100644
--- a/blitzbrowser/src/components/browser-instance.component.ts
+++ b/blitzbrowser/src/components/browser-instance.component.ts
@@ -164,7 +164,7 @@ export class BrowserInstance extends EventEmitter<BrowserInstanceEvents> {
     super();
     this.#browser_pool_service = this.module_ref.get(BrowserPoolService);
     this.#logger = new Logger(`${BrowserInstance.name}|${id}`);
-    this.#user_data_folder = `/home/pptruser/user-data/${id}`;
+    this.#user_data_folder = `${process.env.DATA_DIR || '/data'}/user-data/${id}`;
   }
 
   get vnc_enabled() {
diff --git a/blitzbrowser/src/components/browser-instance.process.ts b/blitzbrowser/src/components/browser-instance.process.ts
index ed6a127..ed9c896 100644
--- a/blitzbrowser/src/components/browser-instance.process.ts
+++ b/blitzbrowser/src/components/browser-instance.process.ts
@@ -1,5 +1,6 @@
 import { ChildProcess, spawn } from 'child_process';
 import * as fsPromise from 'fs/promises';
+import { existsSync, readdirSync } from 'fs';
 import { exit } from 'process';
 import puppeteer, { Browser } from 'puppeteer';
 
@@ -169,36 +170,51 @@ class BrowserInstanceProcess {
     async #createPuppeteerProcess() {
         console.log('Creating puppeteer process');
 
+        const scanExtensions = (dir: string): string[] => {
+            if (!dir || !existsSync(dir)) return [];
+            return readdirSync(dir, { withFileTypes: true })
+                .filter((e) => e.isDirectory() && existsSync(`${dir}/${e.name}/manifest.json`))
+                .map((e) => `${dir}/${e.name}`);
+        };
+        const extensions = [
+            ...scanExtensions(process.env.GLOBAL_EXTENSIONS_DIR ?? ''),
+            ...scanExtensions(process.env.USER_EXTENSIONS_DIR ?? ''),
+        ];
+
         this.#puppeteer_process = await puppeteer.launch({
             headless: false,
             executablePath: this.#browser_executable_path,
             userDataDir: this.#user_data_folder,
             dumpio: true,
             handleSIGTERM: false,
+            ignoreDefaultArgs: ['--disable-extensions'],
             args: [
                 `--remote-debugging-port=${this.#cdp_port}`,
                 '--remote-debugging-address=0.0.0.0',
                 '--no-first-run',
                 '--no-default-browser-check',
                 `--display=${this.#display}`,
-                '--disable-breakpad', // Disables crash reporting
-                '--disable-component-update', // Disables updates to internal components
-                '--disable-print-preview', // Disables print preview
-                '--disable-domain-reliability', // Disables domain reliability service(client-side reliability monitoring system for Google sites)
-                '--disk-cache-dir=/dev/null', // Prevents disk caching
-                '--no-pings', // Disables hyperlink auditing pings
-                '--disable-notifications', // Prevents web push notifications
-                '--disable-features=TranslateUI', // Disables the translate UI
+                '--disable-breakpad',
+                '--disable-component-update',
+                '--disable-print-preview',
+                '--disable-domain-reliability',
+                '--disk-cache-dir=/dev/null',
+                '--no-pings',
+                '--disable-notifications',
+                '--disable-features=TranslateUI',
                 '--disable-background-networking',
                 '--disable-sync',
-                '--disable-extensions',
                 '--disable-signin-promo',
                 '--disable-gpu',
-                '--metrics-recording-only', // Record metrics, but doesn't report them
-                '--disable-features=PersistentHistograms', // Prevents generation of BrowserMetrics files on disk
+                '--metrics-recording-only',
+                '--disable-features=PersistentHistograms',
                 `--start-maximized`,
                 `--proxy-server=http://127.0.0.1:${this.#proxy_server_port}`,
-                this.#disable_shm ? '--disable-dev-shm-usage' : undefined
+                this.#disable_shm ? '--disable-dev-shm-usage' : undefined,
+                ...(extensions.length ? [`--load-extension=${extensions.join(',')}`] : []),
+                ...(extensions.length ? ['--disable-logging'] : []),
+                ...(extensions.length ? ['--log-level=3'] : []),
+                '--no-sandbox',
             ].filter(s => typeof s === 'string'),
             env: {
                 TZ: this.#timezone
diff --git a/blitzbrowser/src/services/browser-version-managers/index.ts b/blitzbrowser/src/services/browser-version-managers/index.ts
index f9462af..a6da56d 100644
--- a/blitzbrowser/src/services/browser-version-managers/index.ts
+++ b/blitzbrowser/src/services/browser-version-managers/index.ts
@@ -1,4 +1,4 @@
-export const BROWSERS_FOLDER = '/blitzbrowser/browsers';
+export const BROWSERS_FOLDER = `${process.env.DATA_DIR || '/data'}/browsers`;
 
 export type Version = 'latest' | 'default' | (string & {});
 
diff --git a/blitzbrowser/src/services/user-data.service.ts b/blitzbrowser/src/services/user-data.service.ts
index 619e2c2..afb3d60 100644
--- a/blitzbrowser/src/services/user-data.service.ts
+++ b/blitzbrowser/src/services/user-data.service.ts
@@ -27,7 +27,7 @@ export abstract class UserDataService {
 @Injectable()
 export class LocalUserDataService extends UserDataService {
 
-    private static readonly USER_DATA_FOLDER = '/blitzbrowser/user-data';
+    private static readonly USER_DATA_FOLDER = `${process.env.DATA_DIR || '/data'}/user-data`;
 
     readonly #logger = new Logger(LocalUserDataService.name);
 
